import React from "react";
import { StyleSheet, Text, View } from "react-native";
import ScreenWrapper from "../hoc/ScreenWrapper";

const Users = () => {
  return (
    <View>
      <Text>Users</Text>
    </View>
  );
};

export default ScreenWrapper(Users);

const styles = StyleSheet.create({});
